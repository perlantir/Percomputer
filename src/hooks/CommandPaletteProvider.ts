"use client";

/**
 * CommandPaletteProvider — adapter re-export for compatibility with
 * `useKeyboardShortcuts.ts` which imports from `./CommandPaletteProvider`.
 */

export { useCommandPalette } from "./CommandPaletteStore";
