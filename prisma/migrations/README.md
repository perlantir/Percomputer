# Prisma Migrations

Migrations are auto-generated by Prisma during deployment.

For initial setup on Supabase:
1. Vercel build runs `prisma db push` which creates all tables
2. Then hit `POST /api/seed` with header `Authorization: Bearer setup-seed-2024`
3. Database is fully populated with demo data
